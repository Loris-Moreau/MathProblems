#pragma once

#include <algorithm>
#include <climits>
#include <iostream>
#include <vector>

using namespace std;

class Common
{
public:
    static bool isEven(int n)
    {
        if (n % 2 == 0)
        {
            return true;
        }
        else
        {
            return false;
        }
    }

    
};
